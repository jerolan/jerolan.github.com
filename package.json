{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "yarn build &&  npx gh-pages -d out -b gh-pages --dotfiles",
    "build": "next build && next export",
    "start": "next dev",
    "serve": "next start"
  },
  "dependencies": {
    "next": "^10.0.0",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.3.0",
    "postcss": "^8.3.5",
    "tailwindcss": "^2.2.4"
  }
}
