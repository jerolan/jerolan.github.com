{
  "name": "jerolan.github.io",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "yarn build && cp -r .well-known dist && npx gh-pages -d dist -b master --dotfiles"
  },
  "dependencies": {
    "autoprefixer": "^9.8.6",
    "parcel-bundler": "^1.12.4",
    "postcss": "^8.2.10",
    "tailwindcss": "^1.6.2"
  }
}
